<!DOCTYPE html>
<html lang="es">
<head>
    <%-include('./partials/head') %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <style>
      *{
        color:white;
      }
    </style>

</head>
<body style="background-color:var(--spaceSoft);">
    <%-include('./partials/header') %>

    <h1 style="margin-left: 10%;">Editar</h1>


    <div class="container">
        <% for( let i=0; i<posts.length;i++ ) { %>
            <form action="/timeline/edit/<%=posts[i].id%>" method="post">
                <% if (idPost == posts[i].id) { %>
                    <div class="form-group">
                        <label for="title">Titulo del post</label>
                        <input required type="text" name="title" id="title" class="form-control" value="<%=posts[i].title%>">
                    </div>
                    <div class="form-group">
                        <label for="description">Descripcion del post</label>
                        <textarea name="description" id="description" class="form-control"><%=posts[i].description%></textarea>
                    </div>
                    <div class="form-group">
                        <label for="content">Post en Markdown</label>
                        <textarea required name="content" id="content" class="form-control" ><%=posts[i].content%></textarea>
                    </div>
                    <br>
                    <input type="submit" value="Enviar" class="btn btn-primary">
                <% } %>
            </form>
        <% } %>
    </div>
</body>
</html>